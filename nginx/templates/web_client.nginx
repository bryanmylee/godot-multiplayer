server {
        # Forward requests for the web client.
        listen [::]:443;
        listen 443;
        server_name {domain};

        location / {
                proxy_pass              http://127.0.0.1:10443;
        }
}

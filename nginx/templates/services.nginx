server {
        # Forward requests.
        listen [::]:8000;
        listen 8000;
        listen [::]:8100;
        listen 8100;
        server_name {domain};

        location / {
                proxy_pass              http://127.0.0.1:1$server_port;
        }
}
